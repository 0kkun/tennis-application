#account-page.account-page
  .account-page__inner.clearfix
    .account-page__inner--left.account-page__header
      %h2 Edit Account
      %h5 アカウントの編集
      %i.btn ログアウト
      = link_to "トップページに戻る", :back, class: 'btn'
    .account-page__inner--right.user-form
      = form_for(current_user) do |f|
        .field
          .field-label
            = f.label :username
          .field-input
            = f.text_field :username, autofocus: true
        .field
          .field-label
            = f.label :email
          .field-input
            = f.email_field :email

        -# .field
        -#   .field-label
        -#     = f.label :Favorite_Player_Name
        -#   .field-input
        -#     = f.text_field :player_id, placeholder: 'Player名を入力してください'

        .actions
          = f.submit "Update", class: 'btn'



  -# = @test
  -# %br
  -# - @players.each do |player|
  -#   = player.player_name_en
  -#   %br
  
  -# = @user.current_user.id
  -# = @favorite_players
  -# - form_for @user do |u|
  -#   = @u.player.player_name_en

  -# - @favorite_players.each do |player|
  -#   = player.player_name_en

  -# = form_for @players do |f|
  -#   = f.collection_check_boxes :player_ids, Player.all, :id, :player_name_en